This directory is mounted into the Nginx container at /etc/nginx/ssl.
Place your TLS certificate files here if you plan to enable HTTPS.

Currently the nginx.conf provided listens on port 80 (HTTP) only.